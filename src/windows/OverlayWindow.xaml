<Window
    x:Class="LiveCaptionsTranscriber.OverlayWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:LiveCaptionsTranscriber"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Overlay Window"
    Width="650"
    Height="135"
    MinWidth="650"
    MinHeight="135"
    AllowsTransparency="True"
    Background="Transparent"
    MouseEnter="Window_MouseEnter"
    MouseLeave="Window_MouseLeave"
    ResizeMode="CanResize"
    ShowInTaskbar="False"
    Topmost="True"
    WindowStyle="None"
    mc:Ignorable="d">

    <Window.Resources>
        <Style x:Key="TransparentThumbStyle" TargetType="Thumb">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Opacity" Value="0" />
        </Style>
    </Window.Resources>

    <Grid>
        <Border
            x:Name="BorderBackground"
            Margin="5"
            Background="#80000000"
            CornerRadius="8"
            MouseLeftButtonDown="Border_MouseLeftButtonDown">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.Resources>
                    <!-- Resources for overlay window -->
                </Grid.Resources>

                <!-- Top Control Bar -->
                <Grid Grid.Row="0" Margin="5,5,5,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    
                    <!-- Settings Controls (Left Side) -->
                    <StackPanel
                        x:Name="ControlPanel"
                        Grid.Column="0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Orientation="Horizontal"
                        Visibility="Hidden">
                        <ui:Button
                            x:Name="FontIncrease"
                            Background="#FFAAAAAA"
                            Click="FontIncrease_Click"
                            Cursor="Hand"
                            Icon="{ui:SymbolIcon fontIncrease20, Filled=False}"
                            ToolTip="Font Increase"
                            Margin="0,0,2,0" />
                        <ui:Button
                            x:Name="FontDecrease"
                            Background="#FFAAAAAA"
                            Click="FontDecrease_Click"
                            Cursor="Hand"
                            Icon="{ui:SymbolIcon fontDecrease20, Filled=False}"
                            ToolTip="Font Decrease"
                            Margin="0,0,2,0" />
                        <ui:Button
                            x:Name="FontColorCycle"
                            Background="#FFAAAAAA"
                            Click="FontColorCycle_Click"
                            Cursor="Hand"
                            Icon="{ui:SymbolIcon color16, Filled=False}"
                            ToolTip="Font Color"
                            Margin="0,0,5,0" />
                        <ui:Button
                            x:Name="OpacityIncrease"
                            Background="#FFAAAAAA"
                            Click="OpacityIncrease_Click"
                            Cursor="Hand"
                            Icon="{ui:SymbolIcon arrowAutofitUp20, Filled=False}"
                            ToolTip="Opacity Increase"
                            Margin="0,0,2,0" />
                        <ui:Button
                            x:Name="OpacityDecrease"
                            Background="#FFAAAAAA"
                            Click="OpacityDecrease_Click"
                            Cursor="Hand"
                            Icon="{ui:SymbolIcon arrowAutofitDown20, Filled=False}"
                            ToolTip="Opacity Decrease"
                            Margin="0,0,2,0" />
                        <ui:Button
                            x:Name="BackgroundColorCycle"
                            Background="#FFAAAAAA"
                            Click="BackgroundColorCycle_Click"
                            Cursor="Hand"
                            Icon="{ui:SymbolIcon color16, Filled=False}"
                            ToolTip="Background Color"
                            Margin="0,0,5,0" />
                        <ui:Button
                            x:Name="ClickThrough"
                            Background="#FFAAAAAA"
                            Click="ClickThrough_Click"
                            Cursor="Hand"
                            Icon="{ui:SymbolIcon lockClosed16, Filled=False}"
                            ToolTip="Click Through" />
                    </StackPanel>
                    
                    <!-- Copy/Clear Buttons (Right Side) -->
                    <StackPanel
                        Grid.Column="1"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Orientation="Horizontal"
                        Visibility="Hidden"
                        x:Name="OverlayControlButtons">
                        <ui:Button
                            x:Name="OverlayCopyButton"
                            Content="Copy"
                            Margin="0,0,5,0"
                            Padding="8,4"
                            Click="OverlayCopyButton_Click"
                            Icon="{ui:SymbolIcon Copy16}"
                            Background="#FFAAAAAA"
                            FontSize="12" />
                        <ui:Button
                            x:Name="OverlayClearButton"
                            Content="Clear"
                            Padding="8,4"
                            Click="OverlayClearButton_Click"
                            Icon="{ui:SymbolIcon Delete16}"
                            Background="#FFAAAAAA"
                            FontSize="12" />
                    </StackPanel>
                </Grid>

                <!-- Full Transcription Display -->
                <ui:Card
                    x:Name="FullTranscriptionCard"
                    Grid.Row="1"
                    Margin="5,5,5,5"
                    Padding="8"
                    VerticalAlignment="Stretch"
                    Background="Transparent"
                    BorderThickness="0">
                    <ScrollViewer
                        x:Name="OverlayScrollViewer"
                        VerticalScrollBarVisibility="Auto"
                        HorizontalScrollBarVisibility="Disabled">
                        <TextBox
                            x:Name="FullTranscriptionText"
                            FontSize="16"
                            Foreground="#FFFFFFFF"
                            Background="Transparent"
                            BorderThickness="0"
                            IsReadOnly="True"
                            TextWrapping="Wrap"
                            VerticalAlignment="Stretch"
                            Text="{Binding FullTranscriptionText, UpdateSourceTrigger=PropertyChanged}"
                            VerticalScrollBarVisibility="Disabled"
                            HorizontalScrollBarVisibility="Disabled"
                            Cursor="IBeam"
                            ContextMenuOpening="FullTranscriptionText_ContextMenuOpening"
                            KeyDown="FullTranscriptionText_KeyDown">
                            
                            <TextBox.ContextMenu>
                                <ContextMenu x:Name="TextContextMenu">
                                    <MenuItem Header="Copy Selected" Click="CopySelected_Click" Icon="{ui:SymbolIcon Copy16}" InputGestureText="Ctrl+C"/>
                                    <MenuItem Header="Copy All" Click="CopyAll_Click" Icon="{ui:SymbolIcon Copy16}" InputGestureText="Ctrl+Shift+C"/>
                                    <Separator/>
                                    <MenuItem Header="Select All" Click="SelectAll_Click" Icon="{ui:SymbolIcon Document16}" InputGestureText="Ctrl+A"/>
                                </ContextMenu>
                            </TextBox.ContextMenu>
                        </TextBox>
                    </ScrollViewer>
                </ui:Card>


            </Grid>
        </Border>

        <Thumb
            x:Name="TopThumb"
            Height="10"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Top"
            Cursor="SizeNS"
            DragDelta="TopThumb_OnDragDelta"
            Style="{StaticResource TransparentThumbStyle}" />
        <Thumb
            x:Name="BottomThumb"
            Height="10"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Bottom"
            Cursor="SizeNS"
            DragDelta="BottomThumb_OnDragDelta"
            Style="{StaticResource TransparentThumbStyle}" />
        <Thumb
            x:Name="LeftThumb"
            Width="10"
            HorizontalAlignment="Left"
            VerticalAlignment="Stretch"
            Cursor="SizeWE"
            DragDelta="LeftThumb_OnDragDelta"
            Style="{StaticResource TransparentThumbStyle}" />
        <Thumb
            x:Name="RightThumb"
            Width="10"
            HorizontalAlignment="Right"
            VerticalAlignment="Stretch"
            Cursor="SizeWE"
            DragDelta="RightThumb_OnDragDelta"
            Style="{StaticResource TransparentThumbStyle}" />
        <Thumb
            x:Name="TopLeftThumb"
            Width="10"
            Height="10"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Cursor="SizeNWSE"
            DragDelta="TopLeftThumb_OnDragDelta"
            Style="{StaticResource TransparentThumbStyle}" />
        <Thumb
            x:Name="TopRightThumb"
            Width="10"
            Height="10"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Cursor="SizeNESW"
            DragDelta="TopRightThumb_OnDragDelta"
            Style="{StaticResource TransparentThumbStyle}" />
        <Thumb
            x:Name="BottomLeftThumb"
            Width="10"
            Height="10"
            HorizontalAlignment="Left"
            VerticalAlignment="Bottom"
            Cursor="SizeNESW"
            DragDelta="BottomLeftThumb_OnDragDelta"
            Style="{StaticResource TransparentThumbStyle}" />
        <Thumb
            x:Name="BottomRightThumb"
            Width="10"
            Height="10"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Cursor="SizeNWSE"
            DragDelta="BottomRightThumb_OnDragDelta"
            Style="{StaticResource TransparentThumbStyle}" />
    </Grid>
</Window>
