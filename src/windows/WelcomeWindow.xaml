<ui:FluentWindow
    x:Class="LiveCaptionsTranscriber.WelcomeWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:LiveCaptionsTranscriber"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Welcome"
    Width="500"
    Height="300"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ExtendsContentIntoTitleBar="True"
    ResizeMode="NoResize"
    WindowBackdropType="Mica"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Grid.Row="0"
            Height="27"
            ShowMaximize="False"
            ShowMinimize="False">
            <ui:TitleBar.Header>
                <ui:TextBlock Padding="15,0,0,0" VerticalAlignment="Center">
                    Welcome
                </ui:TextBlock>
            </ui:TitleBar.Header>
        </ui:TitleBar>

        <StackPanel Grid.Row="1" Background="{ui:ThemeResource SystemFillColorNeutralBackgroundBrush}" VerticalAlignment="Center">
            <TextBlock
                Margin="20"
                HorizontalAlignment="Center"
                FontSize="32"
                FontWeight="Bold"
                Text="Welcome to LiveCaptions Transcriber!"
                TextAlignment="Center" />
            
            <TextBlock
                Margin="20,10,20,30"
                HorizontalAlignment="Center"
                FontSize="16"
                Text="Ready to start transcribing audio in real-time"
                TextAlignment="Center"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

            <ui:Button
                Margin="0,10,0,0"
                Padding="40,12,40,12"
                HorizontalAlignment="Center"
                Click="CloseButton_Click"
                Content="Get Started" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
